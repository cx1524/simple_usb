/**
 * @file usbd_desc.h
 * @brief 描述符管理：注册 / 索引 / 按速度选择 / 配置描述符拼接（纯软件，MCU 无关）
 *
 * 【所属层】第二层 USBD Core 子模块（core）。
 *
 * 【职责】
 *   1. 注册接口：设备描述符、字符串表（语言 ID → 索引 → UTF-16LE）、
 *      配置描述符层级（配置 → 接口 → 端点 → 类特定描述符片段）。
 *   2. 查询接口：按类型/索引/语言 ID 返回对应描述符。
 *   3. **配置描述符拼接**：配置描述符 + 各接口（含类驱动提供的类特定片段）+
 *      各端点描述符拼成完整字节流，wTotalLength 必须与实际总长一致
 *      （最容易写错处，需自检断言）。
 *   4. 分包返回：所有查询按 min(wLength, 总长) 截断。
 *   5. 速度感知：预留按 FS/HS 选择不同描述符集（后续硬件适配阶段启用）。
 *
 * 【需要实现（TODO）】
 *   [ ] 描述符注册表与数据结构
 *   [ ] 注册/查询 API 声明
 *   [ ] 配置描述符拼接逻辑 + wTotalLength 自检
 *   [ ] 类驱动描述符片段钩子（供 usbd_class 挂接）
 *
 * 【依赖】usb_def.h。
 * 【参考】《MCU-USB 启动流程说明》§6.3.4；《USB新手从零搭建框架实践计划》M5。
 */

#ifndef __USBD_DESC_H__
#define __USBD_DESC_H__

/* TODO: 在此定义本文件内容（见上方职责清单） */

#endif /* __USBD_DESC_H__ */
